{
  "name": "rai",
  "private": true,
  "description": "RA(I) — AI Interaction Firewall",
  "workspaces": [
    "packages/core",
    "packages/mcp-connector",
    "packages/p2-agent",
    "packages/extension",
    "packages/mobile-pwa",
    "packages/redteam",
    "packages/telegram-bot"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "regen-arch": "tsx scripts/regen-architecture.ts"
  },
  "devDependencies": {
    "tsx": "^4.22.3"
  }
}
