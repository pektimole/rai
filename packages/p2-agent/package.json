{
  "name": "@rai/p2-agent",
  "version": "0.1.0",
  "description": "RAI P2 — Multi-agent consensus for epistemic verification",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "smoke": "node --env-file=../../.env --import tsx/esm src/smoke-test.ts",
    "smoke:debug": "RAI_DEBUG=1 node --env-file=../../.env --import tsx/esm src/smoke-test.ts",
    "typecheck": "tsc --noEmit"
  },
  "files": ["dist"],
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  }
}
