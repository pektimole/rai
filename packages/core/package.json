{
  "name": "@rai/core",
  "version": "0.1.0",
  "description": "RAI core detection library — P0 (regex) + P1 (Claude API)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run copy-weights",
    "copy-weights": "node -e \"require('fs').cpSync('weights', 'dist/weights', {recursive: true})\"",
    "test": "vitest run"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "js-yaml": "^4.1.1",
    "zod": "^4.3.6"
  }
}
